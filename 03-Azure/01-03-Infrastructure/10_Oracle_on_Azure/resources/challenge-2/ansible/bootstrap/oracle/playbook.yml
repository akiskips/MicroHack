---
- hosts: all
  gather_facts: true
  become: true
  become_method: sudo

  roles:
  - common     # common OS configuration tasks # TODO: add privatekey file for oracle user
  - oracle19c  # oracle software installation/configuration
  - dbca       # database/instance creation #TODO: rename as we are not creating a database here
